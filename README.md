简单的api key调用测试前端页面  
单html文件运行：文档内搜索“const apiKey = "";” 并添加api key后直接在浏览器中打开  

服务器前后端分离运行：  
  1：安装Node.js  
  2：切换到simple-chat-app目录下  
  3：打开cmd，输入npm install  
  4：设置临时环境变量：set OPENAI_API_KEY=（这里填api key）  
  5：启动本地服务器：nmp start  
  6：在浏览器中访问：http://localhost:3000  
